前端的两件事

1，操作页面的元素得到想要的数据
    浏览器提供了 API，叫做 DOM（document object model）
2，发送 HTTP 请求并且处理服务器的返回


JavaScript 的相关知识点
事件绑定
var 声明变量
全局变量
匿名函数
定义函数
object js里面的对象相当于 python 中对象和字典的合体
AJAX 浏览器提供的操作 HTTP 请求的 API


网站会用一种方式提供数据，这个东西叫做服务器的 API
用 JSON 传递数据
js 解析 JSON
模板字符串
插入新元素
修改元素




关于上节课所留的内容中服务器和域名的要点

域名只是方便访问、使用的东西，没什么特殊的

服务器实际上是一台电脑，只不过在远程
用的操作系统当然可以是 windows
但是实际上几乎所有公司都会使用 linux
所以我们肯定也是使用 linux 的
我建议使用 debian 或者 ubuntu
远程服务器当然可以用图形界面
但是实际中 没人会这样做
所以我们也只用 命令行 来操作
我会教如何使用 debian 如何维护、部署项目、装软件等等
